**Program 6: Using StringTokenizer class**

This program demonstrates the basic methods of the `StringTokenizer` class, such as `countTokens()` and `nextToken()`.
 
 **Program 7: StringTokenizer with specific delimiters**

This program shows how to process a sequence of integers separated by commas using a custom delimiter in the `StringTokenizer` constructor.

warning: [removal] Integer(String) in Integer has been deprecated and marked for removal

***Use Integer.parseInt()**

numberArray[counter] = Integer.parseInt(data.nextToken());